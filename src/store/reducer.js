const initialState = {
  cardInfo:{
    cardNo= "",
    expDate="",
    pass="",
    cvv2=""
  },
  emailAdd=""
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
